server {
  listen 80;

  location / {
    root /usr/share/nginx/html;
    index index.html;
  }

  location /upload {
    proxy_pass http://backend:5000;
  }

  location /images {
    proxy_pass http://backend:5000;
  }
}
